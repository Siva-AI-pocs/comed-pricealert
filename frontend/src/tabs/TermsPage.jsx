// Placeholder — full terms copy is ported from app/static/terms.html during
// the cutover task. Public route (no auth).
export default function TermsPage() {
  return (
    <section data-testid="page-terms">
      <h2>Terms of Service</h2>
      <p style={{ color: "var(--dim)" }}>Terms content coming soon.</p>
    </section>
  );
}
