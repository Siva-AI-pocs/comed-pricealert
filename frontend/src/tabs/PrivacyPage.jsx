// Placeholder — full policy copy is ported from app/static/privacy.html during
// the cutover task. Public route (no auth).
export default function PrivacyPage() {
  return (
    <section data-testid="page-privacy">
      <h2>Privacy Policy</h2>
      <p style={{ color: "var(--dim)" }}>Policy content coming soon.</p>
    </section>
  );
}
