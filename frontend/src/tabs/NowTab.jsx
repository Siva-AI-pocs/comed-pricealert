// Placeholder — live price hero, stats, decision banner and charts land in
// the Now-tab task. Routing/shell wire up against this stub.
export default function NowTab() {
  return (
    <section data-testid="tab-now">
      <h2>Right now</h2>
      <p style={{ color: "var(--dim)" }}>Live price coming soon.</p>
    </section>
  );
}
